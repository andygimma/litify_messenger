<h1>Dashboard Home</h1>
<p id="notice"><%= notice %></p>

<h1>Emails</h1>

<table>
  <thead>
    <tr>
      <th>From</th>
      <th>Subject</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @emails.each do |email| %>
      <tr>
        <td><%= email.user.email %></td>
        <td><%= email.subject %></td>
        <td><%= email.body %></td>
        <td><%= link_to 'Show', message_thread_path(email.message_thread_id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= will_paginate @emails %>
<br>

<%= link_to 'New Email', new_email_path %>
